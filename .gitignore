.venv/
__pycache__/
*.pyc
.pytest_cache/
.vscode/

# Perfil real (identidad, evidencia, textos de postulacion). Se publica solo perfil.ejemplo.json
perfil.json

# Legado v1: el empresas.csv de la raiz lo generaba la version anterior.
# El de config/ SI se versiona (el patron con / solo aplica a la raiz).
/empresas.csv

# Exports crudos de n8n: traen ID de Sheet y credenciales.
credenciales*.json
Radar de Vacantes.json
*.export.json

# Dashboard generado por radar.dashboard (derivado de data/, no se versiona)
site/
